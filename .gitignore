# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build de l'exécutable de bureau (Electron / electron-builder)
release
Villainous.lnk

# Fichiers sources GIMP (trop lourds ; on versionne les .png/.jpg exportés)
*.xcf

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cinématique d'intro : suivie via Git LFS (cf. .gitattributes).
# Source brute (>100 Mo) gardée en local seulement.
/assets/Les Méchants Disney se déchaînent !.mp4

# Découpes intermédiaires de préparation d'assets (jetables).
/_crops/
_crop.cjs

# Générateur de cartes Unity (outil externe, ~1,8 Go : .exe + data). Gardé en
# local ; les exports utiles sont copiés dans public/editor/.
/Villainous Card Generator (neutre)/
